export const environment = {
  production: false,
  showGithubActivity: false,
  apiUrl: 'https://your-api-url.example.com',
  // Example placeholder: Firebase config removed from examples. If you need to
  // use Firebase for local experiments, add the config to an untracked file.
};
