export const environment = {
  production: true,
  showGithubActivity: false,
  apiUrl: 'https://your-api-url.example.com',
  // Example placeholder: Firebase config removed from examples. Use local
  // untracked files for private Firebase config if needed.
};
